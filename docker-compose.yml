services:
  xueya:
    build: .
    container_name: xueya
    ports:
      - "8080:8080"
    environment:
      ADDR: ":8080"
      DATA_PATH: "/app/data/records.json"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
